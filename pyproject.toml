[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "prompt_metrics"
version = "0.1.0"
description = "LLM prompt response scoring and evaluation"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
